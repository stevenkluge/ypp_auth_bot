{
  "name": "ypp_pirate_verifier_bot",
  "version": "0.0.1",
  "description": "Discord bot that scrapes pirate data from yoweb to set appropriate server ranks",
  "main": "build/app.js",
  "scripts": {
    "start": "tsc && node build/app.js",
    "build": "tsc",
    "clean": "rm -rf build/**",
    "lint": "eslint src/ --fix && prettier --write",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "git config core.hooksPath ./git_hooks"
  },
  "author": "Steven Kluge",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "@typescript-eslint/parser": "^6.7.5",
    "eslint": "^8.51.0",
    "eslint-config-prettier": "^9.0.0",
    "lint-staged": "^14.0.1",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0"
  }
}
